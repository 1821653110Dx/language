# 定义页边距
```tex
\usepackage{geometry}
\geometry{
a4paper,
left=2.5cm,
right=2.5cm,
top=2.5cm,
bottom=2.5cm,
}
```

# 页眉页脚
```tex
\usepackage{fancyhdr}
\pagestyle{plain} % 取消页眉
\lfoot{}	% 页码出现在下方
```
# 目录
## 生成目录
	\tableofcontents
	
# 标题
```tex
%定义一级二级标题
\ctexset{
	section = {	% 定义一级标题 
		format+ = \zihao{4} \heiti \raggedright,	% 字号为’-4‘，字体为’黑体‘，左对齐
		name = {,、},	% 序号后面加’、‘
		number = \chinese{section},	% 序号为’汉字‘
		beforeskip = 1.0ex plus 0.2ex minus .2ex,	% 标题前的垂直间距 = 基础高度为1.0ex，可以伸展到 1.0 - 0.2 = 1.2ex，也可以收缩到 1.0 - .2 = 0.8ex; ex为当前字号下字母x的高度
		afterskip = 1.0ex plus 0.2ex minus .2ex,	% 标题后的垂直间距 = ...	
		aftername = \hspace{0pt}	% 编号和标题之间的格式 = 增加水平间距’0pt‘ 
	},
	subsection = {	% 定义二级标题
		format+ = \zihao{5} \songti \raggedright,
		name = {,、},
		number = \arabic{subsection},	% 序号为’阿拉伯数字‘
		beforeskip = 1.0ex plus 0.2ex minus .2ex,
		afterskip = 1.0ex plus 0.2ex minus .2ex,
		aftername = \hspace{0pt},
	},
 	subsubsection = {
			format+ = \zihao{5} \songti \raggedright,
			name = {,、},
			number = {\arabic{subsubsection}.\arabic{subsubsection}},	
			beforeskip = 1.0ex plus 0.2ex minus .2ex,
			afterskip = 1.0ex plus 0.2ex minus .2ex,
			aftername = \hspace{0pt},
	}
}
```

# 脚注
## 对于文本
```tex
脚注\footnote[42]{...is that the answer to everything?}	%手动创建编号
\footnote{【正义】：一本云“天下之民，谓之浑沌.”}	%自动创建编号
```
## 对于表格
```tex
\usepackage{threeparttable}

\begin{table}
	\centering
	\begin{threeparttable}[b]
		\caption{caption of the tabel}
		\label{tab:test2}
		%% write table
		\begin{tabular}{llll}
			...
		\end{tabular}
		%% write tablenotes
		\begin{tablenotes}
			\footnotesize
			\item[1] ...
			\item ...
		\end{tablenotes}
	\end{threeparttable}
\end{table}
```

# 行间距
```tex
% 方法1
\linespread{1.5} % 正文1.5倍行距，放在正文区之前

% 方法2
\setlength{\parskip}{0em}	% 段前段后间距为 0
\setlength{\baselineskip}{20pt}	% 正文行间距20磅
\selectfont	% 刷新行间距信息
```

# 首行缩进
```tex
\setlength{\parindent}{1.02cm} % 正文首行悬挂 1.02cm
\setlength{\parindent}{1.02cm} % 正文首行悬挂: 2汉字字符

\hspace{4em}	% 段落开头缩进四个汉字字符

\noindent	% 取消当前段落的缩进
```
# 垂直空白区域
## 2cm
	\vspace{2cm}
## row distance
- 1 \bigskip
- 1/2 \medskip
- 1/4 \smallskip
-
# 对齐
## 把行内的某部分文字右对齐
	\hfill

# 另起一页
	\clearpage


