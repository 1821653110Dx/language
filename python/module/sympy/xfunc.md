# sum
	Sum(expr, (i, 1, n), (j, 1, n) ...)

# log
## unevaluate
	
## evaluate
- ln10
	log(10)
- $log_{2}10$
	log(10, 2)
# else
## $\mathrm{\displaystyle{\prod^n_{i=1}f}}$
```python
prod = 1
f = []
for i in range(__n__) :
	prod *= f[i]
pprint(prod)
```
```python
def prod(list,n) :	## def func prod: {list, n : null}
	product = 1
	f = ls
	for i in range(n) :
			product *= f[i]
	return(product)
```
